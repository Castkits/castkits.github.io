# Headless vs UI

CastKit is split intentionally.

## Headless

Use `@castkit/core` when you want state and behavior but your own UI. Hooks expose wallet connection, network, ENS, minting, token balances, and transaction states.

## UI

Use `@castkit/ui` when you want a ready-made wallet and mint experience with Tailwind and accessible primitives.

