# useWallet

Headless connection lifecycle hook.

## Returns

- address and formattedAddress
- connection flags
- async `connect` and `disconnect`
- error state

## Example

```tsx
const { isConnected, formattedAddress, connect } = useWallet()
```

