# Wallet Connection

Start with `ConnectWalletButton` for default UX, or call `useWallet` directly when the product needs a custom connection surface.

## Recommended Flow

- configure allowed chains
- set `onChainMismatch="prompt-switch"`
- persist connection with the default storage key

