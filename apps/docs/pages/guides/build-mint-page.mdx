# Build a Mint Page

1. Configure an adapter.
2. Wrap the app with `WalletProvider`.
3. Render `MintPanel`.
4. Add project styling with the CastKit Tailwind preset.
5. Replace mock services with production data fetchers as needed.

