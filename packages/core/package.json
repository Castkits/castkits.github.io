{
  "name": "@castkit/core",
  "version": "0.1.0",
  "description": "Headless hooks, contexts, and types for CastKit.",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^18.3.0 || ^19.0.0"
  },
  "dependencies": {
    "react": "^19.1.0",
    "viem": "^2.29.0"
  },
  "devDependencies": {
    "@castkit/tsconfig": "workspace:*"
  }
}

